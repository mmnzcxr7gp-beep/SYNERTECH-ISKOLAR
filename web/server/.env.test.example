# Isolated Test Environment Configuration Template
# Safe placeholders only — never commit credentials to version control

NODE_ENV=test
TEST_MONGO_URI=mongodb://127.0.0.1:27017/iskolar_isolated_test
JWT_SECRET=test-isolated-jwt-secret-256bit-hex-random-placeholder
PORT=4001
ALLOW_TEST_OVERRIDE=true
N8N_DISABLED=true
STORAGE_DRIVER=local
    